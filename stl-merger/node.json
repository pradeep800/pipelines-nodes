{
  "version": "1.0",
  "nodeType": "processing",
  "name": "STL Merger",
  "shortDescription": "Generate a merged STL model as a single STL artifact",
  "description": "Runs the STL merger container and writes a single merged STL file to the pipeline artifacts location. This node does not accept upstream inputs and only requires a unique node name.",
  "githubUrl": "https://github.com/<repo-link>",
  "githubRef": "main",
  "lucideIcon": "Layers",
  "color": "#64748b",
  "hasInput": false,
  "config": {
    "name": {
      "type": "string",
      "label": "Name",
      "description": "Unique identifier for this STL merger node in the pipeline",
      "required": true,
      "unique": true,
      "placeholder": "e.g. merged_stl_model",
      "validation": {
        "patternKey": "snake_case"
      }
    }
  },
  "hasOutput": true,
  "outputDescription": "Single merged STL model file",
  "outputFiles": [
    {
      "name": "merged_model",
      "format": "stl",
      "path": "{{artifacts}}/{{pipeline.name}}/{{execution.timestamp}}/{{node.name}}/merged_model.stl"
    }
  ],
  "implementation": {
    "type": "container",
    "entrypoint": "python merge.py",
    "dockerfilePath": "Dockerfile",
    "dockerContext": "stl-merger"
  }
}
